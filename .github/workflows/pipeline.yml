
on:
  push:
    branches:
      - main
  pull_request:
    branches:
        - main
  workflow_dispatch:

jobs:

##############################################################
  build-and-test:
   runs-on: ubuntu-latest
   
   steps:
    - name: Checkout code
      uses: actions/checkout@v2
  
    - name: Set up .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 8.x
        
    - name: Restore NuGet packages (Main Project)
      run: dotnet restore src/Serverless/**/*.csproj
  
    - name: Build
      run: dotnet build src/Serverless/**/*.csproj

    - name: Restore NuGet packages (Test Project)
      run: dotnet restore TechMedicosAuthTest/*.csproj 
      
    - name: Test
      run: dotnet test TechMedicosAuthTest/*.csproj --no-restore --verbosity normal

##############################################################
  deploy:
     #if: github.ref == 'refs/heads/release' || github.ref == 'refs/heads/main'
     needs: build-and-test
     runs-on: ubuntu-latest
     steps:
      - name: Checkout code
        uses: actions/checkout@v2
    
      - name: Set up .NET
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: 8.x
          
      - name: Login to AWS
        uses: aws-actions/configure-aws-credentials@v3
        with:          
          aws-region: 'us-east-1'
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-session-token: ${{ secrets.AWS_SESSION_TOKEN }}
  
      - name: Configure AWS Dotnet CLI
        run: dotnet tool install -g Amazon.Lambda.Tools
        
      - name: Restore NuGet packages
        run: dotnet restore src/Serverless/**/*.csproj
    
      - name: Build
        run: dotnet build src/Serverless/**/*.csproj --no-restore
  
      - name: Prepare package
  
      - name: Deploy to AWS Lambda auth
      
      - name: Deploy to AWS Lambda cadastro
